- `remove` for unbounded kerosene could be problematic, because
  kerosene can not be withdrawn easily.
- `remove` of unlicensed vaults even if it has assets
- should it be possible to `redeem` kerosene against `dyad`
